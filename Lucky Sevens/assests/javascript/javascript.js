// THIS IS NOT LINKED. 

// I started my javascript over as I was overthinking on my other script that is linked. 


// const bet = document.getElementById("inputBet");
// const saveBet = document.getElementById("startingBet");
let dieOne;
let dieTwo;
let i;
var gameMoney = document.getElementById("Starting Bet");
// var total = document.getElementById("total");

myBtn.addEventListener('click', enterBet);

function enterBet(ev) {
 prompt("How much Money do you have to bet?")
  if (bet.value > 0) {
    saveBet.innerText = bet.value;
    bet.style.display = "none";
  }

  else {
    
    Promt("Please enter value greater than zero");
  }
};

//Check if there is money to play with.


// Program that repeatedly rolls dice.
total = dieOne + dieTwo;
for (i=1; i<gameMoney;i++){

dieOne = math.ceil(math.random()*6);
dieTwo = math.ceil(math.random()*6);

};
// Program that adds & subtracts Money.

// Program that keeps track of rolls before all money is gone.

// Program that keeps track of the roll Count at highest Amount. 

// Program that keeps track of the Highest Amount Won. 
